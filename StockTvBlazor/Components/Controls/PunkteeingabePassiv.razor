<div class="punkte-eingabe">

    <table class="punkte-table">
        <colgroup>
            <col style="width:40%">
            <col style="width:auto">  <!-- Mittlere Spalte nimmt Rest -->
            <col style="width:40%">
        </colgroup>


        <tr>
            <td colspan="3" class="hilfe-cell">
                <small class="hilfe-text">
                    @HilfeText
                </small>
            </td>
        </tr>
        <tr>
            <td class="punkte-links">@LeftPoints</td>

            <td class="punkte-mitte">@InputValue</td>

            <td class="punkte-rechts">@RightPoints</td>
        </tr>
    </table>

</div>

@code {
    // Linker Wert
    [Parameter]
    public string LeftPoints { get; set; } = "0";

    // Mittlerer Input-Wert
    [Parameter]
    public string InputValue { get; set; } = "";

    // Rechter Wert
    [Parameter]
    public string RightPoints { get; set; } = "0";

    // Hilfetext
    [Parameter]
    public string HilfeText { get; set; } = "Zahl eingeben und / oder * drücken";


}