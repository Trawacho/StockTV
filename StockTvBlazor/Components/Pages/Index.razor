@page "/"
@inject NavigationManager Nav
ICH BIN DIE STARTSEITE
@code {
    protected override void OnAfterRender(bool firstRender)
    {
        if (firstRender)
        {
            // Redirect nur hier, nachdem Render abgeschlossen ist
            Nav.NavigateTo("/layouttest1", replace: true);
        }
    }
}