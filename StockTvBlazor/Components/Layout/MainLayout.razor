@page "/"
@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
    {
        // Direkt beim Initialisieren auf /layouttest1 weiterleiten
        Nav.NavigateTo("/layouttest1", replace: true);
    }
}